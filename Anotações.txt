node = servidor para rodar javascript
npm = node package manager 

instalar pacotes.

npm install --global vue-cli ( instalar pra acessar de qualquer lugar os comandos do vue)

vue init webpack-simple call (web pack simple é um template)

babel = transpilar o codigo js 2015 pro antigo.

interpolação = mão única.

mozila javascript 

Uso do this -> 

bind = ligação.

hooks = gancho (created)




